import React from 'react';
import { ContainerHeader } from './Header.styles';

const Header = () => {
  return (
    <ContainerHeader>
      Disney - a memory game with React, Redux and Firebase
    </ContainerHeader>
  )
}

export default Header
